services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2025-latest
    container_name: medwork-sql
    restart: unless-stopped
    environment:
      - ACCEPT_EULA=Y
      - MSSQL_SA_PASSWORD=Sasa1234!
      - MSSQL_PID=Developer
    ports:
      - "11433:1433"
    volumes:
      - sqlserver_data:/var/opt/mssql
    healthcheck:
      test: ["CMD", "bash", "-c", "timeout 5 bash -c '</dev/tcp/localhost/1433' && echo ok"]
      interval: 10s
      timeout: 5s
      retries: 10

  # Container opzionali per deploy/testing containerizzato completo
  backend:
    profiles: ["containers"]
    build:
      context: .
      dockerfile: MedWork.Api/Dockerfile
    container_name: medwork-api
    restart: unless-stopped
    depends_on:
      sqlserver:
        condition: service_healthy
    environment:
      - ASPNETCORE_ENVIRONMENT=Docker
      - ConnectionStrings__DefaultConnection=Server=sqlserver,1433;Database=MedWorkDb;User ID=sa;Password=Sasa1234!;TrustServerCertificate=True;Pooling=False;MultipleActiveResultSets=False;
    ports:
      - "5279:8080"
    volumes:
      - medwork_dataprotection:/app/data-protection-keys

  frontend:
    profiles: ["containers"]
    build:
      context: .
      dockerfile: medwork-frontend/Dockerfile
    container_name: medwork-frontend
    restart: unless-stopped
    depends_on:
      - backend
    environment:
      - VITE_API_BASE_URL=http://localhost:5279
    ports:
      - "5173:80"

volumes:
  sqlserver_data:
  medwork_dataprotection: